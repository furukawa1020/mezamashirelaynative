[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  # Add environment variables in Netlify UI for sensitive keys
  # e.g. FIREBASE_API_KEY = "..."

[dev]
  command = "npm run dev"
  port = 5173
